---
creation date: 2025-12-10 09:02
modification date: 2025-12-10 09:02
---
- **定义**：用任意的存储单元存储数据，通过指针链接。
- **结构**：结点 = 数据域 + 指针域。
- **头结点 (Header Node)**：在第一个数据结点之前附设的一个结点。
    - *作用*：使空表和非空表的处理统一，简化插入/删除操作。
- **关键操作代码 (C语言)**：
    - **插入 (Insert)**：在 $p$ 之后插入 $s$
        ```c
        s->next = p->next; 
        p->next = s; // 顺序不可反，否则丢失后续链表
        ```
    - **删除 (Delete)**：删除 $p$ 之后的结点
        ```c
        q = p->next;
        p->next = q->next;
        free(q);
        ```
- **变体**：
    - **循环链表**：最后一个结点的指针指向头结点。判空条件是 `p->next == head`。
    - **双向链表**：每个结点有两个指针 `prior` 和 `next`。