---
creation date: 2025-12-12 20:13
modification date: 2025-12-12 20:13
---
## 和的分布 ($Z=X+Y$)：卷积公式
若 $X, Y$ 相互独立，则 $Z=X+Y$ 的密度函数为：
$$f_Z(z) = \int_{-\infty}^{+\infty} f_X(x) f_Y(z-x) \,dx$$
或者
$$f_Z(z) = \int_{-\infty}^{+\infty} f_X(z-y) f_Y(y) \,dy$$

> **重要结论 (分布的可加性)**：
> - **二项分布**：$X \sim B(n, p), Y \sim B(m, p) \implies X+Y \sim B(n+m, p)$
> - **泊松分布**：$X \sim P(\lambda_1), Y \sim P(\lambda_2) \implies X+Y \sim P(\lambda_1+\lambda_2)$
> - **正态分布**：$X \sim N(\mu_1, \sigma_1^2), Y \sim N(\mu_2, \sigma_2^2) \implies X+Y \sim N(\mu_1+\mu_2, \sigma_1^2+\sigma_2^2)$

## 最大值与最小值分布 (Order Statistics)
设 $X_1, \dots, X_n$ 相互独立，分布函数均为 $F(x)$。

### 1. 最大值 $M = \max(X_1, \dots, X_n)$
$$F_{max}(z) = P(\text{所有 } X_i \le z) = [F(z)]^n$$
> 逻辑：最大值都小于 $z$，说明每一个都得小于 $z$（“串联”逻辑）。

### 2. 最小值 $N = \min(X_1, \dots, X_n)$
$$F_{min}(z) = 1 - P(\text{所有 } X_i > z) = 1 - [1 - F(z)]^n$$
> 逻辑：最小值都小于 $z$，反面是最小值大于 $z$，即每一个都大于 $z$。

## 一般变换法 (分布函数法)
对于一般的 $Z = g(X,Y)$（如 $Z=X/Y, Z=X^2+Y^2$）：
1. 写出定义：$F_Z(z) = P(g(X,Y) \le z) = \iint_{g(x,y) \le z} f(x,y) \,dx\,dy$。
2. 根据 $z$ 的范围画图，计算二重积分。
3. 求导得 $f_Z(z)$。