# 2021-2022学年第二学期期末B卷

## 一、选择题（每小题2分，共30分）

1. 操作系统是一组（ ）
   A、应用程序
   B、设备驱动程序
   C、系统程序
   D、工具软件

2. 下列关于进程的描述，错误的是（ ）
   A、进程是程序的一次执行
   B、进程可以并发执行
   C、进程是资源分配的基本单位
   D、进程是CPU调度的基本单位

3. 以下关于信号量的描述，错误的是（ ）
   A、信号量是一个整型变量
   B、P操作和V操作是原子操作
   C、信号量只能用于进程同步
   D、信号量初值可以为负数

4. 产生死锁的四个必要条件中，（ ）可以通过破坏来预防死锁
   A、互斥条件
   B、请求与保持条件
   C、不可剥夺条件
   D、循环等待条件

5. 分页存储管理中，页的大小是由（ ）决定的
   A、进程长度
   B、内存容量
   C、硬件结构
   D、用户要求

6. 虚拟存储器的基础是（ ）
   A、局部性原理
   B、程序的顺序执行
   C、多道程序设计
   D、进程的并发执行

7. 段页式存储管理中，要访问一条指令或数据，至少需要访问内存（ ）次
   A、1
   B、2
   C、3
   D、4

8. 在磁盘调度算法中，SCAN算法又称为（ ）
   A、先来先服务算法
   B、最短寻道时间优先算法
   C、电梯算法
   D、循环扫描算法

9. 文件系统中，位示图法可用于（ ）
   A、磁盘空间管理
   B、文件目录管理
   C、文件保护
   D、文件共享

10. I/O控制方式的发展过程中，（ ）方式使得CPU与I/O设备可以并行工作
    A、程序查询
    B、中断
    C、DMA
    D、通道

## 二、判断题（每小题1分，共10分）

1. 进程控制块（PCB）是进程存在的唯一标志。（ ）
2. 虚拟内存的大小可以大于物理内存的大小。（ ）
3. 信号量只能用于实现进程同步，不能用于进程互斥。（ ）
4. 页式存储管理中，每访问一次内存数据需要访问两次内存。（ ）
5. 文件的物理结构决定了文件的逻辑结构。（ ）
6. 死锁是指多个进程因竞争资源而造成的一种僵局现象。（ ）
7. 进程的优先级一旦确定就不能改变。（ ）
8. 缓冲区的引入可以缓解CPU与I/O设备速度不匹配的矛盾。（ ）
9. 在分时系统中，响应时间是衡量系统性能的重要指标。（ ）
10. 设备管理的主要任务之一是实现设备的独立性。（ ）

## 三、填空题（每小题2分，共10分）

1. 操作系统的基本功能包括\\、内存管理、设备管理和文件管理。
2. 进程的基本状态有\\、就绪态和阻塞态。
3. 在请求分页存储管理中，实现虚拟地址到物理地址转换的硬件机构是\\。
4. 磁盘调度的目标是在保证公平性的前提下，使\\最小。
5. 文件的存取方法主要有顺序存取和\\两种。

## 四、综合应用题（每题10分，共50分）

1. 考虑如下段式内存管理机制：逻辑地址的结构如下：
   | 段号（18bit） | 段内地址（14bit） |

   段表结构如下（其中的数字为十进制数字）

   | 段号 | 段长  | 基址  |
   |------|-------|-------|
   | 0    | 8K    | 40K   |
   | 1    | 5K    | 105K  |
   | 2    | 3K    | 10K   |
   | 3    | 2K    | 5K    |
   | 4    | 10K   | 200K  |

   假设逻辑地址为0X10070H，请回答：
   (1) 段号是多少？是否越界？
   (2) 段内偏移是多少？偏移是否超出段长
   (3) 物理地址是多少？

2. 某磁盘共有200个柱面，其编号为0-199，假定磁头刚完成91号柱面的访问，现有一个请求队列在等待访问柱面。该请求队列访问的柱面号分别为：190、97、90、54、180、30、166、108。试比较最短寻道时间有限算法和FIFO扫描算法的磁盘调度，请分别计算磁头移动的总磁道数。（10分）

3. 何谓临界区？下面给出的实现两个进程互斥的算法是安全的吗？为什么？（10分）

```c
#define TRUE 1
#define FALSE 0
int flag[2];
flag[0] = flag[1] = FALSE;

Enter-CriticalSection(int i)
{
    while (flag[1-i]);
    flag[i] = TRUE;
}

Leave-CriticalSection(int i)
{
    flag[i] = FALSE;
}

process i: /* i = 0 or i = 1 */
...
Enter-CriticalSection(i); /* 进入临界区 */
    IN CRITICAL SECTION
Leave-CriticalSection(i); /* 离开临界区 */
...
```

4. 一台计算机有一个cache，主存储器和用作虚拟存储器的磁盘，假设访问cache中的字需要20ns的定位时间。如果该字在主存储器中而不在cache中，则需要60ns的时间载入cache，然后再重新开始定位。如果该字不在主存储器中，则需要12ms的时间从磁盘中提取，然后需要60ns复制到cache中，然后再开始定位。cache的命中率是0.9，主存储器的命中率是0.6，在该系统中访问一个被定位的字所需要的平均时间为多少（单位：ns）?（10分）

5. 设某作业占有7个页面，如果在主存中只允许装入4个工作页面（即工作集为4），作业运行时，实际访问页面的顺序是1,2,3,6,4,7,3,2,1,4,7,5,6,5,2,1。试用FIFO与LRU页面调度算法，列出各自的页面淘汰顺序和缺页中断次数，以及最后留驻主存4页的顺序。（假设开始的4个页面已装入主存）（10分）
